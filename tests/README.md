To run tests, create a virtual environment and run the following commands:

```bash
$ python3 -m pip install -e .
$ python3 -m pip install -r requirements.txt
$ cd tests
```

Then, run the test script that is desired.
